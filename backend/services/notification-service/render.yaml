services:
  - type: web
    name: zurimarket-notification
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main_mongo:app --host 0.0.0.0 --port 8005
